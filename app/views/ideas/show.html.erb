<h2><%= @idea.title %></h2>

<h4><%= @idea.content %></h4>

<%= link_to 'Delete', user_idea_path(current_user, @idea), method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Edit', edit_user_idea_path(current_user, @idea) %> |
<%= link_to 'Back to Your Ideas', user_ideas_path(current_user) %>
